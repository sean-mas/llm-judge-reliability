[project]
name = "faithbench-judge-study"
version = "0.1.0"
description = "Do LLM judges agree with human experts on faithfulness, or reproduce the label distribution?"
requires-python = ">=3.11"
# Gate B runs on the stdlib alone. These are for the run and analysis stages.
dependencies = [
    "pandas>=2.2",
    "pyarrow>=16",
    "httpx>=0.27",
]
